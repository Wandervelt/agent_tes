{
  "name": "techassist-ai",
  "version": "1.0.0",
  "description": "Field Service AI Assistant",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "docker-compose up",
    "build": "docker-compose build",
    "start": "docker-compose up -d"
  }
}